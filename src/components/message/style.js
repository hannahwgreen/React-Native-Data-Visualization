import { StyleSheet } from 'react-native';

const TEXT_COLOR = "#063958";
const MESSAGE_TEXT_SIZE = 30;
const SUBTITLE_TEXT_SIZE = 12;

export default StyleSheet.create({
  messageText: {
    color: TEXT_COLOR,
    fontSize: MESSAGE_TEXT_SIZE
  },
  subtitleText: {
    color: TEXT_COLOR,
    fontSize: SUBTITLE_TEXT_SIZE
  }
});