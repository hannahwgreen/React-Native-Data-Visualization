import { StyleSheet } from 'react-native';

export const DEFAULT_BAR_WIDTH = 40;
export const DEFAULT_MIN_BAR_HEIGHT = 5;

export default StyleSheet.create({
  bar: {
    alignItems: 'center',
    borderRadius: 3,
    justifyContent: 'space-between'
  },
});
